# JAVA_Hightech_20260310
JAVA class contents
================================================
[1] 컴퓨터
(1) Computer ? -> 정보(문자,숫자(정수,실수)로 표현)/처리/시스템
    => 값(정보)를 처리(상태를 바꿈)하는 시스템
((1)) 숫자는 인간이 처리하고자 하는 정보를 컴퓨터는 기계어(0,1)로 저장함(ex. 13은 1101b)
(((1))) 컴퓨터에서 맨앞자리(최상위비트)는 부호비트!
(((2))) 컴퓨터에서 음수를 표현하는 방법은 2의 보수체계(0=+, 1=-) 사용(정수로 표현된값을 전부 뒤바꾼후 1을 더해서 표현함)
(((3))) 컴퓨터에서 실수를 표현하는 방법은 부호비트에 규칙(특정 bit(가상점이 있다고 가정)를 기준으로 앞은 정수 뒤는 실수)을 추가
    (ex. 6.25 -> 0110|01b) / (ex. 0.1 -> 완전히 표현x(얼마나 정밀(근사)하냐에 따라 나눔)
((2)) 문자는 기호로 표현
(((1))) (만든사람들이 127가지로 표현함) = (7bit)+ 최상위비트(부호비트) = 총 8bit가 필요함) => 8bit가 필요하구나! => 8bit=1byte로 표현
     ex1. 00100101 => A라고하자~ => (ASCII)코드!
     ex2. 01010100 = ? => (이 상태에선 알수x) | (숫자로해석)64+16+4 = 84 => (문자로해석)아스키코드표에서 찾아보니 T                         
(2) 추상화(정보를 파편화시켜서 요약하는것) <-> 상세화
(3) 아날로그(연속된값)vs디지털(불연속의 양자화된값)

[2] 컴퓨터 기본구조
(1) HW(물리적존재)
((1)) CPU->(쓰기)->Memory / Memory->(읽기)->CPU
((2)) HW-CPU(연산,제어)  
((3)) HW-Memory()
(((1))) 클락(Clock) : CPU가 일을 진행하는 템포(박자)  / 워드(Word) : CPU가 한번에 처리할수있는 단위

(2) SW(가상존재)
((1)) 메모리(주소,값 저장=1byte(값(데이터)을 저장하는 최소단위)) : 값을 저장하는 공간이 연속적으로 구성 
(((1))) 32bit 컴퓨터 = 4Gbyte(10bit = KB, 20bit = MB, 30bit = GB, 40bit = TB, 50bit = PB...)
(((2))) 사람이 읽고 간결하게 쓰기위해 2진수를 8,16진수로 표현법으로 나타냄
   -> (16진수 표현법)(0x00~0xff) / (8진수 표현법)(0000~7777) 
((2)) 컴퓨터에서 정보가 들어오면 입력, 나가면 출력
((3)) 페이지 기법(Paging) : 가상기억장치(프로그램)와 물리 메모리를 고정된 크기(Page/Frame)로 나누어, 불연속적으로 적재하여 관리하는 운영체제 메모리 관리 방식
   
