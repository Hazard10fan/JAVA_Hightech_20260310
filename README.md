# JAVA_Hightech_20260310
JAVA class contents
================================================
* Computer ? -> 정보(문자,숫자(정수,실수)로 표현)/처리/시스템
    => 값(정보)를 처리(상태를 바꿈)하는 시스템
** 숫자는 인간이 처리하고자 하는 정보를 컴퓨터는 기계어(0,1)로 저장함(ex. 13은 1101b)
*** 컴퓨터에서 맨앞자리(최상위비트)는 부호비트!
*** 컴퓨터에서 음수를 표현하는 방법은 2의 보수체계(0=+, 1=-) 사용(정수로 표현된값을 전부 뒤바꾼후 1을 더해서 표현함)
*** 컴퓨터에서 실수를 표현하는 방법은 부호비트에 규칙(특정 bit(가상점이 있다고 가정)를 기준으로 앞은 정수 뒤는 실수)을 추가
    (ex. 6.25 -> 0110|01b) / (ex. 0.1 -> 완전히 표현x(얼마나 정밀(근사)하냐에 따라 나눔)
** 문자는 기호로 표현
*** (만든사람들이 127가지로 표현함) = (7bit)+ 최상위비트(부호비트) = 총 8bit가 필요함) => 8bit가 필요하구나! => 8bit=1byte로 표현
     ex. 00100101 => A라고하자~ => (ASCII)코드!
* HW(물리적존재) vs SW(가상존재)
** HW-CPU(연산,제어)
* 추상화(정보를 파편화시켜서 요약하는것) <-> 상세화
* 아날로그(연속된값)vs디지털(불연속의 양자화된값)
  
